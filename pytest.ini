# PR-07: Pytest defaults and markers
[pytest]
minversion = 7.0
testpaths = tests
pythonpath = .
addopts = -q
markers =
    LLM_REQUIRED: marks tests that require LLM or network access
    SNAPSHOT: snapshot/golden comparison tests
